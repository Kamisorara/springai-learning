spring:
  application:
    name: springai-demo2
  ai:
    # ?? OpenAI API ???????
    openai:
      api-key: ${OPENAI_API_KEY:your-openai-api-key}  # ??????????API??
      # ??????????????????????base-url
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: qwen3-vl-plus  # ?????????
  cassandra:
    port: 8080